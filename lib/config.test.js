const config = require('./config');

/**
 * Use this to debug the configuration.
 */
describe('config', () => {
    it('log', () => {
        // eslint-disable-next-line no-console
        console.log(config);
    });
});
